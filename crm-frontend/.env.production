# Disable source maps in production so the readable original source is not
# shipped alongside the minified bundles. Without this, anyone can recover the
# full codebase from the browser's DevTools Sources tab.
GENERATE_SOURCEMAP=false

# Pin the API base for production builds.
REACT_APP_API_URL=https://ara-crm-ads-hrarezggb7g7dxdy.southeastasia-01.azurewebsites.net/api
